import SolarController from '@/app/solar/controller/solarController';
import '@testing-library/jest-dom';

// ASYNC NOT SUPPORTED BY JEST 

// describe("Load API data", () => {

//     test("the data matches model", () => {
//         return SolarController(1).then(response => {
//             expect(response.status).toEqual(200);
//             expect(response.message).toHaveProperty("SOLAR");
//         })
//     })
// })